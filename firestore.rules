
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow users to read and write all documents
    // Note: In production, you should restrict access based on roles
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
